import "tfplan/v2" as tfplan

resources = tfplan.resource_changes

invalid = resources.where(
  change.after exists and
  change.after.tags exists and
  (
    not ("Environment" in change.after.tags) or
    not ("Owner" in change.after.tags)
  )
)

main = rule {
  invalid is empty
}
